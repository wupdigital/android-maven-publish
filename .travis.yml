language: android
android:
  components:
    - tools
    - platform-tools
    - build-tools-25.0.3
    - android-25
jdk:
  - oraclejdk8
script:
  - ./gradlew clean check jacocoTestReport coveralls
before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/